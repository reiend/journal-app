<h2>Task list</h2>

<% unless tasks.length.zero? %>
  <ul>
    <% tasks.each do |task| %>
      <li>
        <%= link_to task.name, task_show_path(task) %>
      </li>
    <% end %>
  </ul>
<% else %>
  <div>
    no task yet, add task
  </div>
<% end %>

